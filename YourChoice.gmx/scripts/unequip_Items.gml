//argument0 - unit you want to equip items for
unit = argument0;

if(unit.slotHead != noone){
    item = unit.slotHead;
    unit.baseHitChance -= item.bonusBaseHitChance;
    unit.baseCritChance -= item.bonusBaseCritChance;
    unit.defence -= item.bonusDefence;
    unit.dodgeChance -= item.bonusDodgeChance;
    unit.intellect -= item.bonusIntellect;
    unit.agriculture -= item.bonusAgriculture;
    unit.craftsmanship -= item.bonusCraftsmanship;
    
    unit.maxHitPoints -= item.bonusMaxHitPoints;
    unit.armor -= item.bonusArmor;
    
    unit.sightRadius -= item.bonusSightRadius;
    unit.damageType = item.damageType;
    
    unit.damage -= item.bonusDamage;
    unit.critDamage-= item.bonusCritDamage;
    
    unit.moveRange -= item.bonusMoveRange;
    unit.actionPoints -= item.bonusActionPoints;
    
    unit.reflexes -= item.bonusReflexes;
}

if(unit.slotArmor != noone){
    item = unit.slotArmor;
    unit.baseHitChance -= item.bonusBaseHitChance;
    unit.baseCritChance -= item.bonusBaseCritChance;
    unit.defence -= item.bonusDefence;
    unit.dodgeChance -= item.bonusDodgeChance;
    unit.intellect -= item.bonusIntellect;
    unit.agriculture -= item.bonusAgriculture;
    unit.craftsmanship -= item.bonusCraftsmanship;
    
    unit.maxHitPoints -= item.bonusMaxHitPoints;
    unit.armor -= item.bonusArmor;
    
    unit.sightRadius -= item.bonusSightRadius;
    unit.damageType = item.damageType;
    
    unit.damage -= item.bonusDamage;
    unit.critDamage-= item.bonusCritDamage;
    
    unit.moveRange -= item.bonusMoveRange;
    unit.actionPoints -= item.bonusActionPoints;
    
    unit.reflexes -= item.bonusReflexes;
}

if(unit.slotWeapon1 != noone){
    item = unit.slotWeapon1;
    unit.baseHitChance -= item.bonusBaseHitChance;
    unit.baseCritChance -= item.bonusBaseCritChance;
    unit.defence -= item.bonusDefence;
    unit.dodgeChance -= item.bonusDodgeChance;
    unit.intellect -= item.bonusIntellect;
    unit.agriculture -= item.bonusAgriculture;
    unit.craftsmanship -= item.bonusCraftsmanship;
    
    unit.maxHitPoints -= item.bonusMaxHitPoints;
    unit.armor -= item.bonusArmor;
    
    unit.sightRadius -= item.bonusSightRadius;
    unit.damageType = item.damageType;
    
    unit.damage -= item.bonusDamage;
    unit.critDamage-= item.bonusCritDamage;
    
    unit.moveRange -= item.bonusMoveRange;
    unit.actionPoints -= item.bonusActionPoints;
    
    unit.reflexes -= item.bonusReflexes;
}

if(unit.slotWeapon2 != noone){
    item = unit.slotWeapon2;
    unit.baseHitChance -= item.bonusBaseHitChance;
    unit.baseCritChance -= item.bonusBaseCritChance;
    unit.defence -= item.bonusDefence;
    unit.dodgeChance -= item.bonusDodgeChance;
    unit.intellect -= item.bonusIntellect;
    unit.agriculture -= item.bonusAgriculture;
    unit.craftsmanship -= item.bonusCraftsmanship;
    
    unit.maxHitPoints -= item.bonusMaxHitPoints;
    unit.armor -= item.bonusArmor;
    
    unit.sightRadius -= item.bonusSightRadius;
    unit.damageType = item.damageType;
    
    unit.damage -= item.bonusDamage;
    unit.critDamage-= item.bonusCritDamage;
    
    unit.moveRange -= item.bonusMoveRange;
    unit.actionPoints -= item.bonusActionPoints;
    
    unit.reflexes -= item.bonusReflexes;
}

if(unit.slotItem1 != noone){
    item = unit.slotItem1;
    unit.baseHitChance -= item.bonusBaseHitChance;
    unit.baseCritChance -= item.bonusBaseCritChance;
    unit.defence -= item.bonusDefence;
    unit.dodgeChance -= item.bonusDodgeChance;
    unit.intellect -= item.bonusIntellect;
    unit.agriculture -= item.bonusAgriculture;
    unit.craftsmanship -= item.bonusCraftsmanship;
    
    unit.maxHitPoints -= item.bonusMaxHitPoints;
    unit.armor -= item.bonusArmor;
    
    unit.sightRadius -= item.bonusSightRadius;
    unit.damageType = item.damageType;
    
    unit.damage -= item.bonusDamage;
    unit.critDamage-= item.bonusCritDamage;
    
    unit.moveRange -= item.bonusMoveRange;
    unit.actionPoints -= item.bonusActionPoints;
    
    unit.reflexes -= item.bonusReflexes;
}

if(unit.slotItem2 != noone){
    item = unit.slotItem2;
    unit.baseHitChance -= item.bonusBaseHitChance;
    unit.baseCritChance -= item.bonusBaseCritChance;
    unit.defence -= item.bonusDefence;
    unit.dodgeChance -= item.bonusDodgeChance;
    unit.intellect -= item.bonusIntellect;
    unit.agriculture -= item.bonusAgriculture;
    unit.craftsmanship -= item.bonusCraftsmanship;
    
    unit.maxHitPoints -= item.bonusMaxHitPoints;
    unit.armor -= item.bonusArmor;
    
    unit.sightRadius -= item.bonusSightRadius;
    unit.damageType = item.damageType;
    
    unit.damage -= item.bonusDamage;
    unit.critDamage-= item.bonusCritDamage;
    
    unit.moveRange -= item.bonusMoveRange;
    unit.actionPoints -= item.bonusActionPoints;
    
    unit.reflexes -= item.bonusReflexes;
}
